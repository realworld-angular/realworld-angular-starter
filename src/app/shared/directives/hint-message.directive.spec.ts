import { HintMessageDirective } from './hint-message.directive';

describe('HintMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new HintMessageDirective();
    expect(directive).toBeTruthy();
  });
});
