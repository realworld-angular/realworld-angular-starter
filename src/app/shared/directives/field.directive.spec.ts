import { FieldDirective } from './field.directive';

describe('FieldDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldDirective();
    expect(directive).toBeTruthy();
  });

  // TODO add proper testing with Testing Library
});
